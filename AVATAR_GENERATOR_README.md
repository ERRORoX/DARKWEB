# 🎭 Генератор Аватаров Хакера для DARKWEB

## Описание

Генератор аватаров создает уникальные визуальные идентификаторы для профилей пользователей в стиле хакера/darkweb. Поддерживает несколько стилей и форматов.

## Возможности

### Типы аватаров:

1. **Матричный аватар** - Классический зеленый стиль с матричным паттерном
2. **Аватар со сканированием** - Эффект сканирования с анимацией
3. **Глитч аватар** - Эффект глитча с искажениями
4. **ASCII аватар** - Текстовый ASCII арт
5. **CSS аватар** - Градиентный аватар с эффектами

### Функции:

- ✅ Автоматическая генерация на основе имени пользователя
- ✅ Уникальные цвета для каждого пользователя
- ✅ SVG формат для масштабирования
- ✅ Анимации и эффекты
- ✅ Скачивание аватаров
- ✅ Интеграция с профилем пользователя

## Использование

### 1. Открыть генератор

Откройте файл `html/avatar-generator.html` в браузере.

### 2. Сгенерировать аватар

1. Введите имя пользователя
2. Выберите размер аватара (100-500px)
3. Нажмите "ГЕНЕРИРОВАТЬ АВАТАРЫ"
4. Просмотрите все варианты аватаров
5. Скачайте понравившийся SVG

### 3. Интеграция в профиль

Аватары автоматически генерируются в профиле пользователя при загрузке страницы профиля.

## Программное использование

### JavaScript API

```javascript
// Создание генератора
const generator = new HackerAvatarGenerator();

// Генерация матричного аватара
const matrixAvatar = generator.generateMatrixAvatar('username', 200);

// Генерация ASCII аватара
const asciiAvatar = generator.generateAsciiAvatar('username');

// Генерация CSS аватара
const cssAvatar = generator.generateCSSAvatar('username');

// Генерация аватара со сканированием
const scanAvatar = generator.generateScanAvatar('username', 200);

// Генерация глитч аватара
const glitchAvatar = generator.generateGlitchAvatar('username', 200);
```

### Использование в HTML

```html
<!-- SVG аватар -->
<div id="avatar-container"></div>
<script>
    const generator = new HackerAvatarGenerator();
    const avatar = generator.generateMatrixAvatar('username', 200);
    document.getElementById('avatar-container').innerHTML = avatar;
</script>
```

### CSS классы

```html
<!-- Матричный аватар -->
<div class="avatar-container">
    <div class="avatar-matrix avatar-lg">A</div>
    <div class="avatar-status"></div>
    <div class="avatar-rank elite">E</div>
</div>

<!-- Аватар со сканированием -->
<div class="avatar-scan avatar-md">H</div>

<!-- Глитч аватар -->
<div class="avatar-glitch avatar-lg">X</div>

<!-- Неоновый аватар -->
<div class="avatar-neon avatar-xl">L</div>
```

## Размеры аватаров

- `avatar-sm` - 50x50px (маленький)
- `avatar-md` - 100x100px (средний)
- `avatar-lg` - 150x150px (большой)
- `avatar-xl` - 200x200px (очень большой)

## Статусы

- `avatar-status` - Онлайн (зеленый)
- `avatar-status offline` - Оффлайн (серый)
- `avatar-status afk` - Отошел (оранжевый)

## Ранги

- `avatar-rank` - Обычный ранг (зеленый)
- `avatar-rank elite` - Элитный (красный)
- `avatar-rank legend` - Легендарный (золотой с анимацией)

## Цветовая палитра

Генератор использует следующие цвета:
- **Matrix**: #00ff41 (зеленый)
- **Cyan**: #00fff7 (циан)
- **Red**: #ff4444 (красный)
- **Blue**: #1fc3ff (синий)
- **Purple**: #9d4edd (фиолетовый)
- **Orange**: #ffaa44 (оранжевый)

## Настройка

### Изменение цвета

Измените массив `colors` в классе `HackerAvatarGenerator`:

```javascript
this.colors = {
    matrix: '#00ff41',
    cyan: '#00fff7',
    // добавьте свои цвета
};
```

### Добавление новых стилей

Создайте новый метод в классе `HackerAvatarGenerator`:

```javascript
generateCustomAvatar(username, size = 200) {
    // Ваша логика генерации
    return svgContent;
}
```

## Файлы

- `js/avatar-generator.js` - Основной класс генератора
- `html/avatar-generator.html` - Интерфейс генератора
- `css/avatar.css` - Стили для аватаров
- `js/profile.js` - Интеграция с профилем

## Примеры использования

### Автоматическая генерация в профиле

Аватары автоматически генерируются при загрузке страницы профиля на основе имени пользователя.

### Ручная генерация

```javascript
const generator = new HackerAvatarGenerator();
const user = JSON.parse(localStorage.getItem('darknet_user') || '{}');
const avatar = generator.generateMatrixAvatar(user.username, 150);
document.getElementById('avatar').innerHTML = avatar;
```

### Сохранение типа аватара

```javascript
localStorage.setItem('user_avatar_type', 'matrix');
const avatarType = localStorage.getItem('user_avatar_type');
```

## Технические детали

### Алгоритм генерации

1. Создание хеша из имени пользователя
2. Выбор цвета на основе хеша
3. Генерация уникального паттерна
4. Создание SVG/ASCII/CSS аватара

### Производительность

- Генерация занимает < 10ms
- SVG аватары масштабируются без потери качества
- CSS аватары легкие и быстрые

## Будущие улучшения

- [ ] Поддержка загрузки пользовательских изображений
- [ ] Генерация 3D аватаров
- [ ] Анимации при наведении
- [ ] Интеграция с системой достижений
- [ ] Генерация аватаров для других пользователей
- [ ] Экспорт в PNG/JPG
- [ ] Кастомные эффекты
- [ ] Поддержка темной/светлой темы

## Поддержка

Если у вас возникли проблемы или вопросы:
1. Проверьте консоль браузера на ошибки
2. Убедитесь, что все файлы загружены
3. Проверьте поддержку SVG в браузере

## Лицензия

Часть проекта DARKWEB.


